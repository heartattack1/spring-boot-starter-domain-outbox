outbox:
  enabled: true
  polling:
    enabled: true
    fixed-delay: 1s
    batch-size: 100
    lock-timeout: 5s
  retry:
    max-attempts: 10
    backoff: exponential
    initial: 1s
    max: 1m
  publisher:
    type: none
